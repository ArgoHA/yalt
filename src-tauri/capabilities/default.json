{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the yalt window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
